<Page
    x:Class="Ragna_Rundt.View.Udstillingstemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ragna_Rundt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:Ragna_Rundt.Viewmodel"
    xmlns:view="using:Ragna_Rundt.View"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewmodel:ViewModel>
        </viewmodel:ViewModel>
    </Page.DataContext>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Background="MediumVioletRed">
            <TextBlock Text="{Binding Name, Mode=OneWay}" TextAlignment="Center" FontWeight="Bold" FontSize="30"></TextBlock>
            <Slider x:Name="Slider" Margin="0,50" Maximum="3" TickPlacement="BottomRight" TickFrequency="1" SnapsTo="Ticks" Value="{Binding Key, Mode=TwoWay}"></Slider>
            <WebView x:Name="VideoLink" HorizontalAlignment="Stretch" Height="748" Margin="0,0,0.33,0" VerticalAlignment="Stretch" ></WebView>
            
        </StackPanel>
        <StackPanel Grid.Column="0" Background="blue" Orientation="Vertical">
            <TextBox Margin="10,10,10,10" Text="{Binding Key, Mode=TwoWay}" ></TextBox>
        </StackPanel>

    </Grid>
</Page>
